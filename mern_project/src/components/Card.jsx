import React from "react";

const Card = ({title, image, date}) => {
    return (
    <div>
        <div className="">
            <img src={image} alt=""/>
        </div>
    </div>
    );
};

export default Card;